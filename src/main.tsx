import React from 'react';
import ReactDOM from 'react-dom/client';
import TattingDesigner from './tattingindex.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TattingDesigner />
  </React.StrictMode>
);
